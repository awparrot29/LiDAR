flask
gunicorn
numpy
# Pinned: mediapipe 1.x removed the legacy `mp.solutions` API that
# gait-analysis/pipelandmark.py depends on. 0.10.14 matches the local env.
mediapipe==0.10.14
matplotlib
pandas
# RTMPose tracker. rtmlib runs the same rtmpose-m model on ONNX Runtime, which
# avoids pulling in torch + mmcv + mmdet (~1.5 GB) for ~15 MB instead.
rtmlib
onnxruntime
# Bundles a static ffmpeg with libx264 so the movie is H.264 and plays
# inline in a browser; OpenCV's mp4v output cannot be embedded.
imageio
imageio-ffmpeg
